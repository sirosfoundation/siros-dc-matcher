[package]
name = "siros-dcql"
version = "0.2.0"
description = "Digital Credentials Query Language (DCQL) engine, as defined by OpenID for Verifiable Presentations 1.0"
keywords = ["dcql", "openid4vp", "credentials", "mdoc", "sd-jwt"]
categories = ["authentication", "parser-implementations", "encoding"]
readme = "README.md"
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true

# This crate is published to crates.io and MUST NOT depend on any other crate
# in this workspace. CI enforces it with `cargo publish --dry-run`.
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
